import "../globals.css";
import Metrics from "../components/metrics/metrics";

export default function App({ Component, pageProps }) {
  return (
    <>
      <Metrics />
      <Component {...pageProps} />
    </>
  );
}
